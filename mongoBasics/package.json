{
  "name": "mongobasics",
  "version": "1.0.0",
  "description": "localhost:27017\r It looks like you are trying to access MongoDB over HTTP on the native driver port.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongo": "^0.1.0",
    "mongodb": "^4.5.0"
  }
}
